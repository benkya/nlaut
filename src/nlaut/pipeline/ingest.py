"""L2 输入接入 + 意图澄清桩（M1）。

职责：接收口述/需求片段/API 文档，识别缺失信息（测试目标/被测对象/预期行为），
产出澄清追问清单。缺失信息不猜测——与用户沟通规范一致。
"""


def clarify(raw_input: str) -> list[str]:
    """返回需要向用户确认的问题列表（M1 实现模型调用后自动识别缺口）。"""
    raise NotImplementedError("M1: 意图澄清待实现")
